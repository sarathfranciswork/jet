version: "3.8"

x-app-env : &app-env
  environment:
    - NGINX_PORT=${NGINX_PORT:-84}
    - DEBUG=${DEBUG:-0}
    - DJANGO_SETTINGS_MODULE=${DJANGO_SETTINGS_MODULE:-jet.settings.selfhosted}
    - NEXT_PUBLIC_ENABLE_OAUTH=${NEXT_PUBLIC_ENABLE_OAUTH:-0}
    - NEXT_PUBLIC_DEPLOY_URL=${NEXT_PUBLIC_DEPLOY_URL:-http://localhost/spaces}
    - SENTRY_DSN=${SENTRY_DSN:-""}
    - GITHUB_CLIENT_SECRET=${GITHUB_CLIENT_SECRET:-""}
    - DOCKERIZED=${DOCKERIZED:-1}
    #DB SETTINGS
    - PGHOST=${PGHOST:-jet-db}
    - PGDATABASE=${PGDATABASE:-jet}
    - POSTGRES_USER=${POSTGRES_USER:-jet}
    - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-jet}
    - POSTGRES_DB=${POSTGRES_DB:-jet}
    - PGDATA=${PGDATA:-/var/lib/postgresql/data}
    - DATABASE_URL=${DATABASE_URL:-postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${PGHOST}/${PGDATABASE}}
    # REDIS SETTINGS
    - REDIS_HOST=${REDIS_HOST:-jet-redis}
    - REDIS_PORT=${REDIS_PORT:-6379}
    - REDIS_URL=${REDIS_URL:-redis://${REDIS_HOST}:6379/}
    # EMAIL SETTINGS
    - EMAIL_HOST=${EMAIL_HOST:-""}
    - EMAIL_HOST_USER=${EMAIL_HOST_USER:-""}
    - EMAIL_HOST_PASSWORD=${EMAIL_HOST_PASSWORD:-""}
    - EMAIL_PORT=${EMAIL_PORT:-587}
    - EMAIL_FROM=${EMAIL_FROM:-"Team Jet &lt;team@mailer.jet.so&gt;"}
    - EMAIL_USE_TLS=${EMAIL_USE_TLS:-1}
    - EMAIL_USE_SSL=${EMAIL_USE_SSL:-0}
    - DEFAULT_EMAIL=${DEFAULT_EMAIL:-captain@jet.so}
    - DEFAULT_PASSWORD=${DEFAULT_PASSWORD:-password123}
    # OPENAI SETTINGS
    - OPENAI_API_BASE=${OPENAI_API_BASE:-https://api.openai.com/v1}
    - OPENAI_API_KEY=${OPENAI_API_KEY:-"sk-"}
    - GPT_ENGINE=${GPT_ENGINE:-"gpt-3.5-turbo"}
    # LOGIN/SIGNUP SETTINGS
    - ENABLE_SIGNUP=${ENABLE_SIGNUP:-1}
    - ENABLE_EMAIL_PASSWORD=${ENABLE_EMAIL_PASSWORD:-1}
    - ENABLE_MAGIC_LINK_LOGIN=${ENABLE_MAGIC_LINK_LOGIN:-0}
    - SECRET_KEY=${SECRET_KEY:-60gp0byfz2dvffa45cxl20p1scy9xbpf6d8c5y0geejgkyp1b5}
    # DATA STORE SETTINGS
    - USE_MINIO=${USE_MINIO:-1}
    - AWS_REGION=${AWS_REGION:-""}
    - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID:-"access-key"}
    - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY:-"secret-key"}
    - AWS_S3_ENDPOINT_URL=${AWS_S3_ENDPOINT_URL:-http://jet-minio:9000}
    - AWS_S3_BUCKET_NAME=${AWS_S3_BUCKET_NAME:-uploads}
    - MINIO_ROOT_USER=${MINIO_ROOT_USER:-"access-key"}
    - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD:-"secret-key"}
    - BUCKET_NAME=${BUCKET_NAME:-uploads}
    - FILE_SIZE_LIMIT=${FILE_SIZE_LIMIT:-5242880}

services:
  web:
    <<: *app-env
    platform: linux/amd64
    image: makeplane/jet-frontend:${APP_RELEASE:-latest}
    restart: unless-stopped
    command: /usr/local/bin/start.sh web/server.js web
    deploy:
      replicas: ${WEB_REPLICAS:-1}
    depends_on:
      - api
      - worker

  space:
    <<: *app-env
    platform: linux/amd64
    image: makeplane/jet-space:${APP_RELEASE:-latest}
    restart: unless-stopped
    command: /usr/local/bin/start.sh space/server.js space
    deploy:
      replicas: ${SPACE_REPLICAS:-1}
    depends_on:
      - api
      - worker
      - web

  api:
    <<: *app-env
    platform: linux/amd64
    image: makeplane/jet-backend:${APP_RELEASE:-latest}
    restart: unless-stopped
    command: ./bin/takeoff
    deploy:
      replicas: ${API_REPLICAS:-1}
    depends_on:
      - jet-db
      - jet-redis

  worker:
    <<: *app-env
    container_name: bgworker
    platform: linux/amd64
    image: makeplane/jet-backend:${APP_RELEASE:-latest}
    restart: unless-stopped
    command: ./bin/worker
    depends_on:
      - api
      - jet-db
      - jet-redis

  beat-worker:
    <<: *app-env
    container_name: beatworker
    platform: linux/amd64
    image: makeplane/jet-backend:${APP_RELEASE:-latest}
    restart: unless-stopped
    command: ./bin/beat
    depends_on:
      - api
      - jet-db
      - jet-redis

  jet-db:
    <<: *app-env
    container_name: jet-db
    image: postgres:15.2-alpine
    restart: unless-stopped
    command: postgres -c 'max_connections=1000'
    volumes:
      - pgdata:/var/lib/postgresql/data
    
  jet-redis:
    <<: *app-env
    container_name: jet-redis
    image: redis:6.2.7-alpine
    restart: unless-stopped
    volumes:
      - redisdata:/data

  jet-minio:
    <<: *app-env
    container_name: jet-minio
    image: minio/minio
    restart: unless-stopped
    command: server /export --console-address ":9090"
    volumes:
      - uploads:/export

  createbuckets:
    <<: *app-env
    image: minio/mc
    entrypoint: >
      /bin/sh -c " /usr/bin/mc config host add jet-minio http://jet-minio:9000 \$AWS_ACCESS_KEY_ID \$AWS_SECRET_ACCESS_KEY; /usr/bin/mc mb jet-minio/\$AWS_S3_BUCKET_NAME; /usr/bin/mc anonymous set download jet-minio/\$AWS_S3_BUCKET_NAME; exit 0; "
    depends_on:
      - jet-minio

  # Comment this if you already have a reverse proxy running
  proxy:
    <<: *app-env
    container_name: proxy
    platform: linux/amd64
    image: makeplane/jet-proxy:${APP_RELEASE:-latest}
    ports:
        - ${NGINX_PORT}:80
    depends_on:
      - web
      - api
      - space

volumes:
  pgdata:
  redisdata:
  uploads:
